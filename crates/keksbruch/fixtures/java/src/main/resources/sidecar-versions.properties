# Filled in at build time by Maven resource filtering (see pom.xml properties).
# A shaded jar collapses per-dependency manifests, so the sidecar reads its
# reported library versions from here instead of Package.getImplementationVersion().
tomcat=${tomcat.version}
resteasy=${resteasy.version}
jersey=${jersey.version}
httpclient5=${hc5.version}
