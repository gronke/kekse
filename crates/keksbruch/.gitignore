# Differential-harness fixture dependencies (installed locally, never committed).
.venv/
node_modules/

# .NET sidecar build output.
fixtures/dotnet/bin/
fixtures/dotnet/obj/

# nginx sidecar runtime scratch (conf/logs/temp the resty driver writes per run).
fixtures/.nginx-run/

# The generated matrix outputs — Markdown and CSV.
# Regenerated by every matrix run; none are tracked.
COOKIE_MATRIX.md
COOKIE_MATRIX.csv
