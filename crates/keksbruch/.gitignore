# Differential-harness fixture dependencies (installed locally, never committed).
.venv/
node_modules/

# .NET sidecar build output (bin/obj, and the published folder the harness runs).
fixtures/dotnet/bin/
fixtures/dotnet/obj/
fixtures/dotnet/publish/

# Python sidecar deps installed offline into the bind-mount (pip install --target),
# so the python image can run under --network=none. Built in CI, never committed.
fixtures/.pydeps/

# nginx sidecar runtime scratch (conf/logs/temp the resty driver writes per run).
fixtures/.nginx-run/

# Java sidecar build output (the shaded jar is built in CI / locally, never committed).
fixtures/java/target/
fixtures/java/dependency-reduced-pom.xml

# C/libcurl sidecar build output (the binary is compiled in CI / locally from
# parse_cookies.c with `cc … -lcurl`, never committed).
fixtures/parse_cookies

# The generated HTML report (published to GitHub Pages by CI; only the .md/.csv
# snapshots are tracked). Regenerated by every matrix run.
COOKIE_MATRIX.html

# The generated matrix outputs — Markdown, CSV, and the HTML report.
# Regenerated by every matrix run and published to GitHub Pages by CI; none are tracked.
COOKIE_MATRIX.md
COOKIE_MATRIX.csv
COOKIE_MATRIX.html
