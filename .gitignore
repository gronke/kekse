/target
# gronke/rust-ci checked out at $RUST_CI_REF by the CI workflows (single-sourced ref).
/.rust-ci
# Sealed CARGO_HOME the gronke/rust-ci Docker actions populate.
/.cargo-cache

# Linux core dumps (a crashed bench process would land one in a crate dir)
core.*
